# HYBRID-VALIDATION — stub (sub-referencja)

Pełna logika walidacji hybrydowej znajduje się w pliku kanonicznym (shared/).

**Wczytaj przed użyciem:**
```
view /mnt/skills/user/shared/HYBRID-VALIDATION.md
```

Uruchom zgodnie z instrukcją tamtego pliku — wszystkie fazy, format raportu i sekwencja aktualizacji są tam zdefiniowane.

**Skrót dla tego skilla:**
Po wygenerowaniu dokumentu/raportu/pisma → wczytaj plik główny → wykonaj sekwencję walidacji (raport braków → uzupełnianie → zamknięcie).
