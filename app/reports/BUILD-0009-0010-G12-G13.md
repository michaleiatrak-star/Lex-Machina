# Build 0009/0010 — G12 Dynamic Models + G13 Local HTTP API

Date: 2026-09-15
Branch: `feature/local-runtime`
PR: #38

## Result

**PASS**

### G12

Run: `35000069786`

Dynamic model discovery is provider-side and credential-aware:

- OpenAI: `/v1/models`
- Anthropic: paginated `/v1/models`
- xAI: `/v1/language-models`

Credential material stays server-side and is excluded from returned model descriptors and sanitized errors.

### G13

Run: `35001067932`

Validated SHA: `1f586ab061a858d1b51b109ca1192109772cc836`

The local application backend:

- binds only to loopback;
- exposes health, public skill metadata, DR routes and model discovery;
- never exposes SKILL.md bodies;
- rejects non-local browser origins;
- starts successfully against the full Lex development corpus.

## Local command

```bash
cd app/lex-runtime
npm install
npm start
```

Default: `http://127.0.0.1:4317`

## Next

G14 — local React/Vite UI consuming this API.
