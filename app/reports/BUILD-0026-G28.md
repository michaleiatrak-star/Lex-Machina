# Build 0026 — G28 Local Pseudonymization / Deanonymization

Date: 2026-09-15  
Branch: `feature/local-runtime`  
PR: #38

## Status

**PASS — G28 LOCAL PSEUDONYMIZATION AND DEANONYMIZATION**

## Implemented

- local reversible `PseudonymizationVault`;
- stable tokens such as `[PII:PESEL:0001]`;
- structured Polish identifiers: PESEL, NIP, REGON, Polish IBAN, email and formatted telephone numbers;
- optional local Polish PERSON NER through Stanza;
- local Stanza worker uses cached resources at runtime;
- re-identification values are not JSON-serializable from the vault;
- deanonymization requires the same local vault;
- unknown pseudonym tokens fail closed.

## Validation

- privacy unit tests — **success**
- G28 deterministic gate — **success**
- Python Stanza worker syntax — **success**
- Lex Runtime Validation run `35024309354` — **success**
- validated head: `f9ef6975a2ae860ce9e16ae1b02b5d06a538d336`

## Privacy rule

The re-identification map remains backend-local and is not part of the browser response or audit bundle.
