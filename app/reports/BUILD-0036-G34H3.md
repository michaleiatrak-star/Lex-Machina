# Build 0036 — G34H3 Encrypted Document Persistence

Status: **PASS — G34H3**  
Date: 2026-09-16

## Validated code

Validated code SHA:

`0db39cee7af26d1304f40fb6987af0cb0d320235`

Validation:
- Lex Runtime Validation `35082796671` — **success**
- F-138 structural audit `35082796626` — **success**
- strict TypeScript — **success**
- runtime/app tests — **success**
- G34H1/H2 — **PASS**
- G34H3 encrypted document persistence — **PASS**
- G14-G35 local web UI — **success**
- G17/G19/G20/G22 live probes — **success**

## Implemented

Per case/document:

```text
cases/<caseId>/secure/documents/<documentId>/
  source.lme
  protected.lme
```

`source.lme` contains the local extraction/OCR result only inside LME1.

`protected.lme` contains finalized protected chunks and privacy report only inside LME1.

Properties:
- public documentId remains unchanged for G29/G32 compatibility;
- internal LME objectId is deterministic 128-bit opaque hash of documentId;
- OCR/source clear text is not persisted outside the encrypted blob;
- protected chunks (including any user KEEP selections) are encrypted at rest;
- exact case keyVersion is bound into AAD;
- both blobs participate in CDK rotation;
- restart restore can hydrate source, protected chunks and encrypted privacy vault;
- restored protected chunks can be used without browser re-upload;
- the previous CDK fails after rotation.

## Scope

G34H3 protects persisted source/OCR/protected document representations.

Full G34H remains open:
- G34H4 encrypted artifacts;
- G34H5 legacy plaintext migration/removal.

Raw HTTP request bodies are still memory-buffered by Express before encrypted persistence. This is a memory-pressure limitation, not an at-rest plaintext-file fallback.
