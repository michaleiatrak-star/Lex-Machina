# Build 0011 — G14 Local Web UI

Date: 2026-09-15
Branch: `feature/local-runtime`
PR: #38

## Result

**PASS**

Validated web pipeline: GitHub Actions run `35001673144`.

The current branch additionally restores the exact DR-09 blob from PR #39; F-138 structural audit run `35001719693` passes and current runtime G1-G13 run `35001719829` passes before G14.

## Implemented application shell

- React 19 + Vite local web application in `app/lex-web`;
- localhost-only Vite dev/preview server;
- runtime health indicator;
- OpenAI / Anthropic / xAI provider selector;
- dynamic model selector backed by the local runtime API;
- 16-DR routing selector;
- route validation through the local backend;
- local-first privacy/status messaging;
- responsive application layout.

## Browser security boundary

The production bundle gate rejects browser artifacts containing:

- `OPENAI_API_KEY`;
- `ANTHROPIC_API_KEY`;
- `XAI_API_KEY`;
- `PRAWO-HARDGATE.md`;
- raw `# SKILL:` prompt material;
- private-key markers.

The browser talks only to the local API at `127.0.0.1:4317` by default. Provider credentials and Lex skill bodies remain in the runtime process.

## Deliberate limitation

The `Uruchom analizę` button remains disabled. G14 validates the application shell and safe browser/runtime boundary only; it does not claim end-to-end live provider execution from the UI.

## Next

G15 — execution endpoint and UI session flow: validated route + selected provider/model + user query → Lex execution engine → audited/finalized response. Live credentialled provider validation remains a separate gate.
