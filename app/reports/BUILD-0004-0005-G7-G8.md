# Build 0004/0005 — G7 Vertical Slice + G8 HARD GATE

Date: 2026-09-15
Branch: `feature/local-runtime`
PR: #38

## Result

**PASS**

GitHub Actions run: `34998379446`

Validated SHA:

`9d340232f279a286eb1ef15289e4fdf5d2d01f98`

## G7 — deterministic legal vertical slice

The runtime enforces:

`prawny-router-v3 → prawo-polskie-v2 → ROUTING-MAP.md → one DR → provider`

The full-corpus test uses DR-02 and executes the identical orchestration contract for deterministic OpenAI, Anthropic and xAI adapters.

This gate tests orchestration only. It does not claim substantive legal correctness and does not execute a live provider.

## G8 — HARD GATE finalization

The runtime now refuses to silently finalize detected legal references without verification provenance.

Outcomes:

- supported + VERIFIED ledger + visible verification marker → PASS;
- attempted but unresolved + UNVERIFIED ledger + visible warning → DEGRADED;
- missing ledger record → BLOCKED;
- hidden verification state → BLOCKED.

The initial detector covers article references, Dz.U. references and case signatures. It is intentionally fail-closed and will be expanded before production.

## Next

G9 — append-only audit trail and completeness validation.
