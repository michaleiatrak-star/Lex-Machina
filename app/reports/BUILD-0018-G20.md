# Build 0018 — G20 Official PDF Verification

Date: 2026-09-15  
Branch: `feature/local-runtime`  
PR: #38

## Result

**PASS**

Validation:

- GitHub Actions run: `35014644845` — success
- validated SHA: `360aeb3854810022222a85ea624182becdd0eb86`
- F-138 structural audit: `35014644693` — success

The same workflow passed:

- G1-G20 deterministic validation;
- G17 live official-source probe;
- G19 live temporal freshness probe;
- G20 live official PDF probe;
- G14-G20 local web UI.

## Implemented

- backend-only PDF text extraction with `pdfjs-dist`;
- no OCR;
- no shell / external process execution;
- bounded source size, page count and extracted text size;
- malformed/no-text PDF fails closed;
- official HTTPS host allowlist retained;
- official PDF title + exact legal-reference verification;
- PDF verification ledger provenance:
  - `verificationMethod = web_fetch_pdf`;
  - `sourceFormat = PDF`;
- audit and export logs retain source format;
- production local server enables the bounded PDF verifier by default.

## Temporal integration

G19 states:

- `CURRENT_TEXT_REQUIRES_PDF`;
- `HISTORICAL_TEXT_REQUIRES_PDF`

may proceed only when the configured verifier reports PDF capability.

Without PDF support these states remain denied.

All other non-current temporal states remain fail-closed.

## Acceptance

Deterministic positive path:

- freshness selects current PDF;
- bounded PDF extraction produces text;
- expected act title found;
- requested article found;
- ledger VERIFIED;
- G8 PASS;
- answer released.

Deterministic negative path:

- PDF extraction fails/no text;
- no verification ledger record;
- response BLOCKED;
- no answer released.

Live path:

- real source: `https://api.sejm.gov.pl/eli/acts/DU/2026/795/text.pdf`;
- local PDF extraction executed;
- `art. 5 KC` verified against the official current t.j.;
- source format recorded as PDF;
- no model-provider credentials used.

## Next

**G21 — Official Case-Law Verification**

Close the remaining G8 gap for `sygn.` references using court-family-specific official resolvers, beginning with Sąd Najwyższy.
