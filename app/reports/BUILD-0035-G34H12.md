# Build 0035 — G34H1/G34H2 Encrypted Case Intake

Status: **PASS — G34H1/G34H2**  
Date: 2026-09-16

## Validated code

Validated code SHA:

`32bb2106c1293448e5ae8fbca1b3d416d27d0d7d`

Validation:
- Lex Runtime Validation `35082276687` — **success**
- F-138 structural audit `35082276492` — **success**
- strict TypeScript — **success**
- runtime/app tests — **success**
- G34H1/H2 encrypted incoming + ZIP members — **PASS**
- G14-G35 local web UI — **success**
- G17/G19/G20/G22 live probes — **success**

## G34H1 — encrypted incoming

Production upload intake now uses LME1:
- AES-256-GCM;
- per-purpose HKDF-SHA256 key from case CDK;
- caseId/objectId/purpose/keyVersion AAD;
- opaque upload directory ids;
- original filename only inside encrypted manifest;
- payload and metadata verify before atomic rename;
- multi-chunk file encryption/rekey;
- case CDK rotation rekeys stored upload payloads/manifests.

The legacy plaintext LocalCaseFileStore upload path remains only as compatibility/test/migration fallback. Production server passes SecureCaseUploadStore.

## G34H2 — encrypted ZIP members

ZIP processing:
- archive itself is encrypted at rest;
- plaintext archive exists only in a private 0700 work job directory;
- extraction uses the existing traversal/symlink/bomb-limited Python worker;
- every extracted file is immediately persisted as an opaque `file_<id>/payload.lme`;
- original relative path/member metadata are encrypted in `manifest.lme`;
- persistent extracted tree contains no clear filenames;
- private plaintext job directory is removed in finally;
- startup cleanup removes orphaned ZIP workdirs;
- CDK rotation rekeys all extracted payloads/manifests.

## HTTP cutover

The secure path is used by:
- `POST /api/cases/:caseId/files`;
- `POST /api/documents/review`;
- `POST /api/documents/ingest`;
- `GET /api/cases/:caseId/files`.

ACL is checked before CDK unwrap. CDK exists only inside `withCaseDataKey` callbacks.

## Explicit limits

Full G34H is **not PASS**.

Open:
- G34H3 encrypted OCR/source + protected document persistence;
- G34H4 encrypted artifacts/sensitive export persistence;
- G34H5 legacy plaintext migration;
- case display metadata/auth DB still require separate sensitivity review;
- Express raw upload body still memory-buffers the request before encrypted write.

Shared firm template originals remain plaintext and are a separate application-scope at-rest item.

## Next stage

G34H3:
- persist extracted/OCR source locally only as encrypted LME1;
- persist finalized protected chunks/privacy report as encrypted LME1;
- no clear OCR/protected text in persistent case tree;
- support local reload primitives for later case reopen/stored-file processing;
- include document LME objects in CDK rotation.
