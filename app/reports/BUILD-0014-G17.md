# Build 0014 — G17 Live Official Source Probe

Date: 2026-09-15  
Branch: `feature/local-runtime`  
PR: #38

## Status

**PASS — G17 LIVE OFFICIAL SOURCE PROBE**

Validated implementation SHA:

`2a259ae3d88f345f7fab0b3fd4ead3c9482a8da8`

Validation:
- Lex Runtime Validation run `35004092983` — **success**
- F-138 structural audit run `35004093951` — **success**

## Live source

The probe used the real public Sejm ELI endpoint:

`https://api.sejm.gov.pl/eli/acts/DU/1964/93/text.html`

Reference tested:

`art. 5 KC`

Expected act title:

`Kodeks cywilny`

## Observed result

- network attempts: **1**
- source matched: **true**
- verification status: **VERIFIED**
- source tier: **R1**
- evidence snippet present: **true**
- provider credentials required: **false**
- model-provider API call executed: **false**

## Architecture

G17 is intentionally a separate CI job from deterministic G1-G16 validation.

This separates:
- deterministic runtime regressions;
- real official-source transport/content availability.

The overall workflow requires both layers to pass.

## Security properties retained

- credential-free HTTPS;
- official-host allowlist;
- no redirects;
- 15-second request timeout;
- exact act-title check;
- exact reference check;
- no model-provider secret or call required.

## CI topology

Run `35004092983`:

1. `G1-G16 deterministic validation` — PASS
2. `G17 live official-source probe` — PASS
3. `G14-G16 local web UI` — PASS

## Significance

G16 proved the verification protocol using deterministic HTTP fixtures.

G17 proves that the same verifier works against the current real Sejm/ELI transport and content shape.

The legal-source path is therefore validated end-to-end at the network boundary, independently from model-provider credentials.

## Next gate

**G18 — Deterministic Legal Source Resolver**

Current verification tooling still requires the model to provide an official source URL and expected act title.

G18 should move source selection into runtime:

`legal claim / act identity → runtime resolver → canonical official URL/title → verifier`

The model should request verification by legal identity, not invent transport URLs.
