# Build 0007 — G10 Export Gate

Date: 2026-09-15
Branch: `feature/local-runtime`
PR: #38

## Result

**PASS**

GitHub Actions run: `34999378196`

Validated SHA:

`9bee977e1c3e6a1bcb9517b68db12c64b8155a7f`

## Enforced export properties

Automatic DOCX/PDF export requires:

1. a successful HYBRID-VALIDATION state;
2. G8 finalization = PASS;
3. G9 audit structurally complete before close;
4. SHA-256 generation for the final artifact;
5. a `document_generated` audit event;
6. successful G10 gate event;
7. append-only session close.

G8 `DEGRADED` is deliberately insufficient for automatic export. It requires a future explicit human-decision path.

## Provider-neutral verification log

The runtime emits the documented neutral shape:

`{ session_id, events[] }`

Events retain the verification method, claim context, source URL/tier, status, timestamp and tool-call id.

## Legacy-tool discrepancy

The development corpus documents:

- `shared/tools/walidator_cytowan.py`;
- `shared/tools/extract_api_verification_log.py`;
- `shared/tools/export_gate.py`.

The files themselves are not present under those paths in the current corpus. G10 therefore implements the documented contract natively in the application runtime and reports this condition explicitly instead of claiming that missing scripts ran.

## Separate F-138 result

PR #39 fixes the DR-09 declared count from 35 to 36. Existing F-138 structural audit run `34998972041` passes.

## Next

G11 — deterministic contract matrix covering all 16 DR skills and core execution skills.
