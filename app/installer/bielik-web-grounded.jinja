{%- set lex_policy = "LEX_WEB_GROUNDED_POLICY_V1\nFor factual/current/source questions use real web_research/web_search/web_fetch tool calls before answering; never describe a tool call instead of making it and never invent URLs or source contents. Cite fetched sources inline and list exact returned URLs. LEX_LEGAL_SKILLS_AUTO_POLICY_V1: every legal question starts with a real legal_auto_route call, then legal_skill_read for all mandatory and relevant selected skills; Polish law always loads prawny-router-v3, prawo-polskie-v2, shared and the relevant DR module. LEX_DIRECT_LEGAL_MCP_POLICY_V1: after skill routing use the direct source MCPs prawo_*, saos_*, nsa_*, isap_*, krs_*, eureka_*, kio_*, uodo_*, eu_sparql_*, eu_compliance_* and legalize_* whenever relevant, preferring official sources and retrieving the actual record/document before quoting it. Never send case facts, uploaded document text, secrets, clear PII or PII tokens to external source MCPs; use only neutral public legal queries and identifiers. LEX_PRIVATE_DOCUMENT_POLICY_V1: for sensitive scans use private_ocr_anonymize; for clear text use private_anonymize and draft only with PII tokens. Use private_inflection_contract for PERSON grammar and suffix tokens with |nom, |gen, |dat, |acc, |inst, |loc or |voc. Finalize tokenized text with private_finalize_text or DOCX/ODT with private_finalize_document; clear values must remain local and must not be echoed to the model. If inflection cannot be generated by Morfeusz/SGJP, rewrite to nominative rather than guessing. Clear the local vault when finished. Never claim a search, skill read, source read or tool execution without a real tool result in the current conversation. If a mandatory dependency or source cannot be verified, fail closed and say what failed instead of filling gaps from memory. Respond in the user's language." %}
{{ bos_token }}
{%- if messages[0]['role'] == 'system' %}
    {%- if "LEX_WEB_GROUNDED_POLICY_V1" in messages[0]['content'] %}
        {%- set system_message = messages[0]['content'] %}
    {%- else %}
        {%- set system_message = lex_policy + "\n\n" + messages[0]['content'] %}
    {%- endif %}
    {%- set loop_start_index = 1 %}
{%- else %}
    {%- set system_message = lex_policy %}
    {%- set loop_start_index = 0 %}
{%- endif %}

{%- if system_message or tools %}
    {{- '<|im_start|>system\n' }}
    {%- if system_message %}
        {{- system_message }}
    {%- endif %}
    {%- if tools %}
        {{- '\n\nMasz dostęp do następujących narzędzi. Definicje narzędzi znajdują się poniżej wewnątrz znaczników <|function_list|>:\n<|function_list|>\n' }}
        {{- '[' }}
        {%- for tool in tools %}
            {{- tool | tojson }}
            {%- if not loop.last %}
                {{- ',\n' }}
            {%- endif %}
        {%- endfor %}
        {{- ']\n<|function_list|>\n\nAby wywołać narzędzie, użyj formatu <tool_call>{"name": "nazwa_narzędzia", "arguments": {"argument": "wartość"}}</tool_call>. Wyniki działania narzędzi zostaną przekazane z markerem <|function_output|>. Gdy użytkownik prosi o sprawdzenie, wyszukanie, OCR, anonimizację lub źródło, wykonaj odpowiednie narzędzie zamiast opisywać, jak można je uruchomić.\n' }}
    {%- endif %}
    {%- if enable_thinking %}
        {{- '\n\nZanim odpowiesz na pytanie, najpierw przemyśl swoje kroki i umieść swoje myśli wewnątrz tagów <think>...</think>. Musisz najpierw pomyśleć, zanim udzielisz odpowiedzi. ' -}}
        {{- 'WAŻNE: Powinieneś myśleć w tym samym języku, co pytanie użytkownika. Jeśli pytanie jest zadane po polsku, powinieneś również myśleć po polsku. Jeśli pytanie jest po angielsku, myślisz również po angielsku itd. ' }}
        {{- '** PAMIĘTAJ! ** Pytanie po polsku -> myślenie po polsku -> odpowiedź po polsku!' -}}
        {{- '*** BARDZO WAŻNE!!! *** Jesteś Bielikiem, polskim modelem językowym. Twoją główną cechą jest umiejętność pisania po polsku. Jeśli użytkownik zadaje Ci pytania po polsku, ZAWSZE odpowiadaj po polsku. ' -}}
        {{- 'Nawet, jeśli korzystasz z narzędzia, którego większość instrukcji jest po angielsku, powinieneś przede wszystkim odpowiadać po polsku, jeśli użytkownik zadaje pytanie w tym języku. ' -}}
    {%- endif %}
    {{- '<|im_end|>\n' }}
{%- endif %}

{%- for message in messages[loop_start_index:] %}
    {%- if message['role'] == 'user' %}
        {{- '<|im_start|>user\n' + message['content'] + '<|im_end|>\n' }}
    {%- elif message['role'] == 'assistant' %}
        {{- '<|im_start|>assistant\n' }}
        {%- set content = message.content | default('') %}
        {%- set reasoning_content = message.reasoning_content | default('') %}
        {%- if not reasoning_content and '<think>' in content and '</think>' in content %}
            {%- set reasoning_parts = content.split('</think>') %}
            {%- set reasoning_content = reasoning_parts[0].split('<think>')[-1] %}
            {%- set content = reasoning_parts[1:] | join('</think>') %}
        {%- endif %}
        {%- if reasoning_content %}
            {{- '<think>\n' + reasoning_content.strip() + '\n</think>\n' }}
        {%- endif %}
        {{- content.lstrip() }}
        {%- if message.tool_calls %}
            {%- for tool_call in message.tool_calls %}
                {%- if tool_call.function %}
                    {%- set tool_call = tool_call.function %}
                {%- endif %}
                {{- '\n<tool_call>\n{"name": "' + tool_call.name + '", "arguments": ' + (tool_call.arguments if tool_call.arguments is string else tool_call.arguments | tojson) + '}\n</tool_call>' }}
            {%- endfor %}
        {%- endif %}
        {{- '<|im_end|>\n' }}
    {%- elif message['role'] == 'tool' %}
        {%- if loop.index0 == 0 or messages[loop.index0 - 1]['role'] != 'tool' %}
            {{- '<|im_start|>user\n' }}
        {%- endif %}
        {{- '<|function_output|>' + message['content'] }}
        {%- if loop.last or messages[loop.index0 + 1]['role'] != 'tool' %}
            {{- '<|im_end|>\n' }}
        {%- endif %}
    {%- endif %}
{%- endfor %}

{%- if add_generation_prompt %}
    {{- '<|im_start|>assistant\n' }}
    {%- if enable_thinking %}
        {{- '<think>\n' }}
    {%- endif %}
{%- endif %}
